/**
 * Asynchronous tasks can exchange tokens (permits and messages) using various protocols.
 * This exchange hapeens not between the nodes, but between particular connectors, belonging to nodes.
 * Each subpackage of this package contains a set of interfaces and implementations of connectors for some protocol.
 */
package org.df4j.core.boundconnector;
